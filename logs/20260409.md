# Dev Log — 2026-04-09

## What we did

### Source folder restructure
- Moved all source files from the project root into three named subdirectories:
  - **`model/`**: `PedalZone.h`, `PedalInterpreter.h`, `PedalInterpreter.cpp`
  - **`controller/`**: `App.h`, `App.cpp`, `MidiInputHandler.h`, `MidiInputHandler.cpp`, `PlaybackController.h`, `PlaybackController.cpp`
  - **`voiceover/`**: `Reader.h`, `Reader.cpp`
- `main.cpp`, `RtMidi.h`, `RtMidi.cpp` remain in the project root

### CMakeLists.txt updates
- Updated source paths in `add_executable` to reflect new subdirectory locations
- Added all subdirectories to `target_include_directories` so existing `#include "FileName.h"` directives required no changes

### Git
- Committed restructure on `feature/project-structure`
- Merged into `main` with `--no-ff`
